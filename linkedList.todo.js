class Node {
  constructor(value, next) {}
}

class LinkedList {
  constructor(...values) {}

  push = (value) => {};

  delete = (value) => {};

  /**
   * Insert at index
   * @param {*} value
   * @param {*} index
   * @returns
   */
  insert = (value, index) => {};

  has = (value) => {};

  search = (value) => {};

  values = () => {};

  shift = () => {};
}

module.exports = { LinkedList, Node };
